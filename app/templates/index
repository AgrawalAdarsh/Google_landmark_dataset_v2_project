<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Landmark Detection | Silent Wolf</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      background: linear-gradient(135deg, #74ebd5 0%, #ACB6E5 100%);
      color: #333;
    }
    header {
      background: rgba(255, 255, 255, 0.9);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
    }
    header h2 {
      margin: 0;
      color: #2196F3;
    }
    nav a {
      margin-left: 1rem;
      text-decoration: none;
      color: #333;
      font-weight: 600;
      transition: color 0.3s;
    }
    nav a:hover {
      color: #2196F3;
    }
    .hero {
      text-align: center;
      padding: 4rem 2rem;
    }
    .hero h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }
    .hero p {
      font-size: 1.1rem;
      color: #444;
    }
    .section {
      padding: 3rem 2rem;
      background: white;
      margin: 2rem auto;
      border-radius: 16px;
      width: 90%;
      max-width: 900px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }
    .upload-box {
      text-align: center;
    }
    input[type="file"] {
      display: none;
    }
    label {
      display: inline-block;
      padding: 10px 20px;
      background: #4CAF50;
      color: white;
      border-radius: 8px;
      cursor: pointer;
      margin-bottom: 1rem;
      transition: background 0.3s ease;
    }
    label:hover {
      background: #45a049;
    }
    #preview {
      max-width: 100%;
      border-radius: 12px;
      margin-bottom: 1rem;
      display: none;
    }
    button {
      padding: 10px 20px;
      background: #2196F3;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1rem;
      transition: background 0.3s ease;
    }
    button:hover {
      background: #1976D2;
    }
    #result {
      margin-top: 1rem;
      font-weight: 600;
      color: #333;
    }
    .about-me {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 2rem;
      text-align: left;
    }
    .about-me img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }
    footer {
      text-align: center;
      padding: 1rem;
      background: rgba(255, 255, 255, 0.9);
      margin-top: 2rem;
      font-size: 0.9rem;
      color: #555;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <h2>Landmark Detection</h2>
    <nav>
      <a href="#project">Project</a>
      <a href="#upload">Try It</a>
      <a href="#about">About Me</a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <h1>AI Landmark Detection</h1>
    <p>Upload an image and let AI detect the landmark instantly.<br>
    Built using Google Landmark Dataset v2 & Deep Learning.</p>
  </section>

  <!-- About Project -->
  <section class="section" id="project">
    <h2>📌 About the Project</h2>
    <p>
      This project uses the <b>Google Landmark Dataset v2</b> to train a deep learning model for landmark recognition. 
      A custom CNN built with <b>Keras & TensorFlow</b> processes input images and predicts famous landmarks worldwide. 
      The model has been optimized with early stopping, augmentation, and evaluation metrics to improve accuracy.
    </p>
  </section>

  <!-- Upload Section -->
  <section class="section upload-box" id="upload">
    <h2>🖼️ Try It Yourself</h2>
    <form id="uploadForm" enctype="multipart/form-data">
      <label for="fileInput">Choose an Image</label>
      <input type="file" id="fileInput" name="file" accept="image/*">
      <br>
      <img id="preview" src="" alt="Image Preview">
      <br>
      <button type="submit">Predict</button>
    </form>
    <div id="result"></div>
  </section>

  <!-- About Me -->
  <section class="section about-me" id="about">
    <img src="{{ url_for('static', filename='images/profilepic.jpeg') }}" alt="Your Photo">
    <div>
      <h2>👨‍💻 About Me</h2>
      <p>
        Hi, I’m <b>Adarsh Agrawal</b>, a B.Tech IT student at <b>SGSITS Indore</b>.  
        Passionate about <b>AI, Machine Learning, and Web Development</b>, I love building 
        projects that merge deep learning with real-world applications.
      </p>
      <p>
        Connect with me on:
        <a href="https://github.com/AgrawalAdarsh" target="_blank">GitHub</a> |
        <a href="https://www.linkedin.com/in/adarsh-agrawal-3b0a76268/" target="_blank">LinkedIn</a>
      </p>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    © 2025 Silent Wolf | Built with ❤️ using Python, Flask & TensorFlow
  </footer>

  <script>
    const fileInput = document.getElementById("fileInput");
    const preview = document.getElementById("preview");
    const form = document.getElementById("uploadForm");
    const resultDiv = document.getElementById("result");

    fileInput.addEventListener("change", () => {
      const file = fileInput.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = e => {
          preview.src = e.target.result;
          preview.style.display = "block";
        };
        reader.readAsDataURL(file);
      }
    });

    form.addEventListener("submit", async (e) => {
      e.preventDefault();
      resultDiv.innerText = "⏳ Predicting...";
      const formData = new FormData();
      formData.append("file", fileInput.files[0]);

      const res = await fetch("/predict", {
        method: "POST",
        body: formData
      });
      const data = await res.json();
      resultDiv.innerText = "✅ Predicted Class: " + data.predicted_class;
    });
  </script>

</body>
</html>
